# Copy this file to .env and fill in your key, then run scripts/start-<os>.
# .env is gitignored and never copied into the image - the start scripts pass
# it to the container at run time with `docker run --env-file`.

# Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=
